{
  "name": "Tarefa",
  "properties": {
    "anexos": {
      "description": "JSON array de {nome, url, data} \u2014 lista de arquivos anexados",
      "type": "string"
    },
    "checklist": {
      "description": "JSON array de {id, texto, checked}",
      "type": "string"
    },
    "checklist_template_id": {
      "type": "string"
    },
    "cliente_cpf": {
      "description": "CPF do cliente (denormalizado)",
      "type": "string"
    },
    "cliente_id": {
      "description": "ID do cliente",
      "type": "string"
    },
    "cliente_nome": {
      "description": "Nome do cliente (denormalizado)",
      "type": "string"
    },
    "cliente_telefone": {
      "description": "Telefone do cliente (denormalizado)",
      "type": "string"
    },
    "concluida_por_id": {
      "type": "string"
    },
    "concluida_por_nome": {
      "type": "string"
    },
    "conversa_id": {
      "description": "ID da conversa do WhatsApp vinculada",
      "type": "string"
    },
    "criado_por_id": {
      "type": "string"
    },
    "criado_por_nome": {
      "type": "string"
    },
    "data_cadastro": {
      "description": "Data de cadastro",
      "format": "date",
      "type": "string"
    },
    "data_conclusao_prevista": {
      "description": "Data prevista de conclus\u00e3o",
      "format": "date",
      "type": "string"
    },
    "data_conclusao_real": {
      "description": "Data real de conclus\u00e3o",
      "format": "date",
      "type": "string"
    },
    "descricao": {
      "description": "Descri\u00e7\u00e3o da tarefa",
      "type": "string"
    },
    "empresa_id": {
      "description": "ID da empresa",
      "type": "string"
    },
    "microtarefa": {
      "default": false,
      "description": "Identifica uma a\u00e7\u00e3o r\u00e1pida criada no Bate-papo",
      "type": "boolean"
    },
    "origem": {
      "default": "manual",
      "description": "Origem da tarefa",
      "enum": [
        "manual",
        "sistema",
        "cliente",
        "whatsapp"
      ],
      "type": "string"
    },
    "pendencia_com": {
      "description": "Quem est\u00e1 com a pend\u00eancia",
      "enum": [
        "cliente",
        "banco",
        "administradora",
        "seguradora",
        "detran",
        "cartorio",
        "parceiro",
        "equipe_interna"
      ],
      "type": "string"
    },
    "prioridade": {
      "default": "media",
      "description": "Prioridade",
      "enum": [
        "baixa",
        "media",
        "alta",
        "urgente"
      ],
      "type": "string"
    },
    "prioridade_destaque": {
      "default": false,
      "description": "Marca a tarefa com estrela como prioridade operacional",
      "type": "boolean"
    },
    "responsaveis_fotos": {
      "description": "JSON array de fotos dos respons\u00e1veis",
      "type": "string"
    },
    "responsaveis_ids": {
      "description": "JSON array de IDs dos respons\u00e1veis",
      "type": "string"
    },
    "responsaveis_nomes": {
      "description": "JSON array de nomes dos respons\u00e1veis",
      "type": "string"
    },
    "responsavel_principal_id": {
      "type": "string"
    },
    "responsavel_principal_nome": {
      "type": "string"
    },
    "senha_gov": {
      "description": "Senha GOV do cliente",
      "type": "string"
    },
    "setor_id": {
      "description": "ID do setor da tarefa",
      "type": "string"
    },
    "setor_nome": {
      "description": "Nome do setor (denormalizado)",
      "type": "string"
    },
    "status": {
      "default": "a_fazer",
      "description": "Status da tarefa",
      "type": "string"
    },
    "subsetor_id": {
      "description": "ID do subsetor (substitui tipo_id)",
      "type": "string"
    },
    "subsetor_nome": {
      "description": "Nome do subsetor (substitui tipo_nome)",
      "type": "string"
    },
    "tipo_id": {
      "description": "ID do tipo de tarefa (legado - usar subsetor_id)",
      "type": "string"
    },
    "tipo_nome": {
      "description": "Nome do tipo de tarefa (legado - usar subsetor_nome)",
      "type": "string"
    },
    "titulo": {
      "description": "T\u00edtulo da tarefa",
      "type": "string"
    },
    "vencimento_em": {
      "description": "Data e hora limite da microtarefa",
      "format": "date-time",
      "type": "string"
    }
  },
  "required": [
    "empresa_id",
    "titulo",
    "data_conclusao_prevista"
  ],
  "security": {
    "create": {
      "rule": "if (user.perfil === 'super_admin' || user.perfil === 'master') return true; if (!user.empresa_id) return false; return record.empresa_id === user.empresa_id;"
    },
    "delete": {
      "rule": "if (user.perfil === 'super_admin' || user.perfil === 'master') return true; if (!user.empresa_id) return false; return ['admin','gerente'].includes(user.perfil) && record.empresa_id === user.empresa_id;"
    },
    "read": {
      "rule": "if (user.perfil === 'super_admin' || user.perfil === 'master') return true; if (!user.empresa_id) return false; if (user.perfil === 'parceiro') return record.criado_por_id === user.user_id; return record.empresa_id === user.empresa_id;"
    },
    "update": {
      "rule": "if (user.perfil === 'super_admin' || user.perfil === 'master') return true; if (!user.empresa_id) return false; if (user.perfil === 'parceiro') return record.criado_por_id === user.user_id; return record.empresa_id === user.empresa_id;"
    }
  },
  "type": "object"
}